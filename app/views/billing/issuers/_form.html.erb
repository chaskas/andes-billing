<%= form_with(model: issuer) do |form| %>
  <% if issuer.errors.any? %>
    <div style="color: red">
      <h4><%= pluralize(issuer.errors.count, "error") %> prohibited this issuer from being saved:</h4>

      <ul>
        <% issuer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row mb-3 justify-content-md-left">
    <div class="col-2">
      <%= form.label :tax_number, "Tax Number", class: "col-form-label fw-bold" %>
      <%= form.text_field :tax_number, class: "form-control" %>
    </div>

    <div class="col">
      <%= form.label :name, class: "col-form-label fw-bold" %>
      <%= form.text_field :name, class: "form-control" %>
    </div>
  </div>

  <div class="row mb-3 justify-content-md-left">
    <div class="col-2">
      <%= form.label :phone, class: "col-form-label fw-bold" %>
      <%= form.text_field :phone, class: "form-control" %>
    </div>

    <div class="col">
      <%= form.label :address, class: "col-form-label fw-bold" %>
      <%= form.text_field :address, class: "form-control" %>
    </div>
  </div>

  <div class="row mb-3 justify-content-md-left">
    <div class="col">
      <%= form.label :email, class: "form-label fw-bold" %>
      <%= form.text_field :email, class: "form-control" %>
    </div>
  </div>

  <div>
    <%= form.submit :Submit, class: "btn btn-success mt-3 mb-3" %>
  </div>
<% end %>
